# Editor shell v3

- fixed 100vh editor shell without page-level scrolling;
- remove the remaining legacy top control stack;
- keep page strip fixed at the bottom;
- move booklet settings and export into the page inspector;
- restore functional text insertion and selection;
- restore functional line insertion and selection;
- keep existing project, print, booklet and persistence models unchanged.
