# Template assets

This folder is reserved for reusable template assets.

Planned structure:

```text
public/template-assets/backgrounds/
public/template-assets/decor/
public/template-assets/previews/
```

Use it for backgrounds, decorative PNG/SVG files, and preview thumbnails used by `public/templates/index.json`.
