name: Apply live photo project

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.head_ref == 'agent/photo-blob-storage' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/photo-blob-storage
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply live photo project patch
        run: python scripts/apply_live_photo_project.py
      - name: Install dependencies
        run: npm ci
      - name: Validate patch
        run: |
          npm run lint
          npm test
          npm run build
          git diff --check
      - name: Commit patched editor
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/AppLive.jsx
          if git diff --cached --quiet; then
            echo "Patch already applied"
            exit 0
          fi
          git commit -m "Build portable photo snapshots from live state"
          git push origin HEAD:agent/photo-blob-storage
